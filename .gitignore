# Node / React (client)
client/node_modules/
client/dist/
client/.env
client/.env.local

# PHP (api)
api/vendor/
api/.env

# General
.DS_Store
*.log

# Secrets
api/config/jwt.php
api/config/sms.php
api/config/database.php
ppscpi_lms/sad
ppscpi_lms/.VSCodeCounter

# Uploaded documents — never commit real or test files
api/uploads/*
!api/uploads/.gitkeep